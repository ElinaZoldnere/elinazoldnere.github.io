source 'https://rubygems.org'

gem "jekyll", "~> 4.4.1"

gem "jekyll-remote-theme"   # for local builds using remote_theme
gem "jekyll-seo-tag"        # for local builds using remote_theme
gem "jekyll-include-cache"  # for local builds using remote_theme
